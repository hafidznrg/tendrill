/**
 * Contoh teks di panel home row beranda (ADR-039). Teks latihan tidak di-hardcode
 * di komponen (CLAUDE.md §3 poin 3) — termasuk yang hanya hiasan.
 * Hanya tombol home row Unit 1, supaya tidak menjanjikan tombol yang belum diajarkan.
 */
export const HERO_SAMPLE = { typed: 'fjfj dkdk ', rest: 'jf kd fdjk' } as const;
